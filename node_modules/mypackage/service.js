var mypackage; // namespace

/**
 * Implements the server side Service interface.
 * @apram {object} ns The namespace of this package.
 */
module.exports = function(ns) {
  mypackage = ns;
  this.perform = perform;
};

/**
 * @param {object|array} modelObjects
 * @param {object} moat
 * @param {object} device
 * @param {object} dmjob
 */
function perform(modelObjects, moat, device, dmjob) {
  // TODO
}